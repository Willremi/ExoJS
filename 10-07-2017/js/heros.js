var pointPV = function(agility, force, endurance) {
  return parseInt(agility) + parseInt(force) + parseInt(endurance);
}

if (pointPV == 100) {
 //afficher le héros
}
else {
  // afficher "recommencer"
}
